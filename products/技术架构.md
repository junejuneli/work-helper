# AI秘书 - 技术架构文档

> 版本：v1.0
> 更新时间：2025-10-19

---

## 1. 核心理念

**极简原则**：
- ✅ 使用 @ant-design/x 实现与 Linda 的聊天界面
- ✅ 使用 Semi Design 搭建整体 UI 框架
- ✅ 直接调用 [Claude Code SDK](https://docs.claude.com/en/api/agent-sdk/typescript)
- ❌ 不做复杂的 Agent 协同
- ❌ 不做状态管理和数据库

**技术架构**：
```
React + Semi Design (整体框架)
        ↓
@ant-design/x (聊天界面)
        ↓
Electron IPC
        ↓
Claude Code SDK (AI能力)
```

---

## 2. 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Electron** | 32.x | 桌面应用框架 |
| **React** | 18.x | UI框架 |
| **TypeScript** | 5.x | 开发语言 |
| **Vite** | 6.x | 构建工具 |
| **pnpm** | 9.x | 包管理器 |
| **Semi Design** | 2.x | 整体UI组件库 |
| **@ant-design/x** | 最新 | 聊天界面组件 |
| **@anthropic-ai/claude-code** | 2.0.22 | AI能力SDK |
| **Node.js** | 20.x LTS | 运行时 |

---

## 3. 项目结构

```
work-helper/
├── package.json              # 项目配置
├── tsconfig.json             # TypeScript配置
├── vite.config.ts            # Vite配置
├── .env                      # 环境变量
│
├── electron/                 # Electron主进程
│   ├── main.ts              # 主进程入口
│   ├── preload.ts           # 预加载脚本
│   └── services/
│       └── claude.ts        # Claude SDK调用
│
├── src/                      # React渲染进程
│   ├── main.tsx             # 入口文件
│   ├── App.tsx              # 根组件（Semi Design布局）
│   │
│   ├── pages/
│   │   └── Chat/            # 聊天页面（@ant-design/x）
│   │       └── index.tsx
│   │
│   └── services/
│       └── ipc.ts           # IPC通信封装
│
└── products/                # 文档目录
    ├── 产品文档.md
    ├── 技术架构.md
    └── CLAUDE_CODE_深度分析文档.md
```

---

## 4. 核心实现

### 4.1 整体布局（Semi Design）

**src/App.tsx**：

```typescript
import React from 'react';
import { Layout } from '@douyinfe/semi-ui';
import { Chat } from './pages/Chat';

const { Header, Content } = Layout;

export const App: React.FC = () => {
  return (
    <Layout style={{ height: '100vh' }}>
      <Header style={{ backgroundColor: 'var(--semi-color-bg-1)' }}>
        <h1>AI秘书</h1>
      </Header>

      <Content style={{ padding: '24px' }}>
        <Chat />
      </Content>
    </Layout>
  );
};
```

### 4.2 聊天界面（@ant-design/x）

**src/pages/Chat/index.tsx**：

```typescript
import React, { useState, useEffect, useRef } from 'react';
import { Conversations } from '@ant-design/x';

interface Message {
  key: string;
  placement: 'start' | 'end';
  content: string;
  avatar: string;
}

export const Chat: React.FC = () => {
  const [messages, setMessages] = useState<Message[]>([]);
  const streamingContentRef = useRef('');

  // 监听Claude响应
  useEffect(() => {
    const handleMessage = (data: any) => {
      if (data.type === 'stream') {
        // 流式内容追加
        streamingContentRef.current += data.content;

        setMessages(prev => {
          const newMessages = [...prev];
          const lastMsg = newMessages[newMessages.length - 1];

          if (lastMsg && lastMsg.placement === 'start') {
            lastMsg.content = streamingContentRef.current;
          } else {
            newMessages.push({
              key: Date.now().toString(),
              placement: 'start',
              content: streamingContentRef.current,
              avatar: '🤖'
            });
          }
          return newMessages;
        });
      } else if (data.type === 'complete') {
        streamingContentRef.current = '';
      }
    };

    window.electronAPI.onMessage(handleMessage);
    return () => window.electronAPI.removeListener('claude:message', handleMessage);
  }, []);

  // 发送消息
  const handleSend = async (message: string) => {
    if (!message.trim()) return;

    setMessages(prev => [...prev, {
      key: Date.now().toString(),
      placement: 'end',
      content: message,
      avatar: '👤'
    }]);

    streamingContentRef.current = '';
    await window.electronAPI.sendMessage(message);
  };

  return (
    <Conversations
      items={messages}
      onSubmit={handleSend}
      inputPlaceholder="和 Linda 对话..."
      style={{ height: '100%' }}
    />
  );
};
```

### 4.3 Electron 主进程

**electron/main.ts**：

```typescript
import { app, BrowserWindow, ipcMain } from 'electron';
import path from 'path';
import { ClaudeService } from './services/claude';

const claudeService = new ClaudeService();

function createWindow() {
  const mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false
    }
  });

  if (process.env.NODE_ENV === 'development') {
    mainWindow.loadURL('http://localhost:5173');
  } else {
    mainWindow.loadFile(path.join(__dirname, '../dist/index.html'));
  }

  return mainWindow;
}

app.whenReady().then(() => {
  const mainWindow = createWindow();

  ipcMain.handle('claude:send', async (event, message: string) => {
    await claudeService.sendMessage(message, event.sender);
  });
});

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit();
});
```

**electron/preload.ts**：

```typescript
import { contextBridge, ipcRenderer } from 'electron';

contextBridge.exposeInMainWorld('electronAPI', {
  sendMessage: (message: string) => ipcRenderer.invoke('claude:send', message),
  onMessage: (callback: (data: any) => void) =>
    ipcRenderer.on('claude:message', (_, data) => callback(data)),
  removeListener: (channel: string, callback: any) =>
    ipcRenderer.removeListener(channel, callback)
});
```

**electron/services/claude.ts**：

```typescript
import { query } from '@anthropic-ai/claude-code';
import type { WebContents } from 'electron';

export class ClaudeService {
  async sendMessage(message: string, webContents: WebContents) {
    const conversation = query({
      prompt: message,
      options: {
        cwd: process.cwd(),
        includePartialMessages: true
      }
    });

    for await (const msg of conversation) {
      if (msg.type === 'stream_event') {
        const event = msg.event;
        if (event.type === 'content_block_delta' && event.delta?.text) {
          webContents.send('claude:message', {
            type: 'stream',
            content: event.delta.text
          });
        }
      } else if (msg.type === 'assistant') {
        webContents.send('claude:message', { type: 'complete' });
      }
    }
  }
}
```

---

## 5. 配置文件

### package.json

```json
{
  "name": "ai-secretary",
  "version": "1.0.0",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "dev:electron": "tsc -p tsconfig.node.json && electron .",
    "build": "tsc && vite build",
    "build:electron": "tsc -p tsconfig.node.json"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@douyinfe/semi-ui": "^2.65.1",
    "@ant-design/x": "^1.0.0",
    "@anthropic-ai/claude-code": "^2.0.22"
  },
  "devDependencies": {
    "electron": "^32.2.6",
    "vite": "^6.0.3",
    "typescript": "^5.7.2",
    "@vitejs/plugin-react": "^4.3.4"
  }
}
```

### vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: { port: 5173 }
});
```

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "lib": ["ES2022", "DOM"],
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "moduleResolution": "bundler"
  },
  "include": ["src/**/*"]
}
```

### tsconfig.node.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "outDir": "dist-electron",
    "strict": true,
    "esModuleInterop": true
  },
  "include": ["electron/**/*"]
}
```

### .env

```bash
# Anthropic API Key
ANTHROPIC_API_KEY=your_api_key_here

# 开发环境
NODE_ENV=development
```

---

## 6. 开发流程

### 安装依赖

```bash
pnpm install
```

### 启动开发

```bash
# 终端1：启动Vite
pnpm dev

# 终端2：启动Electron
pnpm dev:electron
```

### 构建打包

```bash
pnpm build
pnpm build:electron
```

---

## 7. 技术要点

| 特性 | 实现方式 |
|------|----------|
| **AI能力** | Claude Code SDK |
| **聊天界面** | @ant-design/x Conversations |
| **整体布局** | Semi Design Layout |
| **桌面应用** | Electron |
| **构建工具** | Vite |
| **流式响应** | includePartialMessages |

---

**核心原则**：
- ✅ 最少依赖
- ✅ 极简实现
- ✅ 流式体验

**预计开发周期**：1周完成MVP
